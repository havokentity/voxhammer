# assets/

Voxel models (`.vox`), palettes, and FMOD banks. Cooked into the engine's
internal format by `tools/assetcook`. Empty in the skeleton pass — content
lands alongside the `.vox` importer (M1) and audio (M6).
